# Legacy References

This directory is intentionally kept as a lightweight placeholder for historical project references that may be useful during maintenance or migration work.

The original legacy projects, generated artifacts, build outputs, IDE metadata, screenshots, and other repository bloat were removed during repository cleanup.

Do not place active application code here. Production development belongs under `app/`.

Add only small, intentionally selected historical references here when they provide useful context for future maintenance.
